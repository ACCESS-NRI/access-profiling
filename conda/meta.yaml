{% set data = load_setup_py_data(setup_file='../setup.py', from_recipe_dir=True) %}
{% set version = data.get('version') %}
{% set pyproj = load_file_data('../pyproject.toml', from_recipe_dir=True) %}
{% set project = pyproj.get('project') %}

package:
    name: access-profiling
    version: "{{ version }}"

build:
    noarch: python
    number: 0
    script: "{{ PYTHON }}  -m pip install . -vv"
    entry_points:
        {% for name, script in project.get('scripts', {}).items() %}
        - {{ name }} = {{ script }}
        {% endfor %}

source:
    path: ../

requirements:
    host:
        - python
        - pip
        - setuptools >=61.0.0
        - setuptools_scm
    run:
        - python >=3.10
        {% for dep in project.get('dependencies', []) %}
        - {{ dep }}
        {% endfor %}

test:
    imports:
        - access.profiling

about:
    home: https://github.com/ACCESS-NRI/access-profiling/
    license: Apache Software
    license_family: APACHE
    summary: "Tools and utilities to profile the ACCESS climate models."
